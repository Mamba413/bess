bess.R:  Best subset selection with a specified model size for generalized linear models and Cox's proportional hazard model.

coef.bess.R:  This function provides estimated coefficients from a fitted "bess" object.

deviance.bess.R:  This function provides deviance from a fitted "bess" object.

LogLik.bess.R:  This function provides loglikelihood from a fitted "bess" object.

plot.bess.R:  Produces a coefficient profile plot of the coefficient or loss paths for a fitted "bess" object of algorithm type "PDAS" and method "sequential".

predict.bess.R:  This function provides predictions from a fitted "bess" object.

print.bess.R:  Print the primary elements of the "bess" object.

